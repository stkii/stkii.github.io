[project]
name = "stkii-github-io"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "pyxel>=2.9.8",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff>=0.16.1",
    "ty>=0.0.65",
]
